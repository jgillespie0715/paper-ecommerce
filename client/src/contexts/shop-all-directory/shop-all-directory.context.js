import { createContext } from 'react';

import SHOPALL_DIRECTORY_DATA from './shop-all-directory.data';

const ShopAllDirectoryContext = createContext(SHOPALL_DIRECTORY_DATA);

export default ShopAllDirectoryContext;
